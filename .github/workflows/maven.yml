name: Java CI

on:
  push:
    branhes:
    - develop

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 12
        uses: actions/setup-java@v1
        with:
          java-version: 12
      - name: Build with Maven
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "<settings><servers><server><id>github</id><username>x-access-token</username><password>e7d95f942f6d4f68f23245ad7ddcdb02cafb99a4</password></server></servers></settings>" > ~/.m2/settings.xml
          mvn -B package --file pom.xml -DskipTests -Dmaven.test.skip=true
          mvn deploy -Dregistry=https://maven.pkg.github.com/cube-io-corp -Dtoken="e7d95f942f6d4f68f23245ad7ddcdb02cafb99a4"
