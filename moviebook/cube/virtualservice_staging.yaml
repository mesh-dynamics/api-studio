---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cubeui-staging
  labels:
    env: staging
spec:
  hosts:
  - staging.cubecorp.io
  gateways:
  - moviebook-gateway
  http:
  - match:
    - uri:
        exact: /
    - uri:
        prefix: /assets
    - uri:
        regex: '^.*\.(ico|png|jpg|js|css)$'
    route:
    - destination:
        host: cubeui.staging.svc.cluster.local
        port:
          number: 80

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cubews-record-staging
  labels:
    env: staging
spec:
  hosts:
  - staging.cubecorp.io
  gateways:
  - moviebook-gateway
  http:
  - match:
    - uri:
        prefix: /cs
    route:
    - destination:
        host: cubews-record.staging.svc.cluster.local
        port:
          number: 8080

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cubews-record-staging-test
  labels:
    env: staging
spec:
  hosts:
  - staging.cubecorp.io
  gateways:
  - moviebook-gateway
  http:
  - match:
    - uri:
        prefix: /cube/cs
    rewrite:
        uri: /cs
    route:
    - destination:
        host: cubews-record.staging.svc.cluster.local
        port:
          number: 8080

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cubews-mock-staging
  labels:
    env: staging
spec:
  hosts:
  - staging.cubecorp.io
  gateways:
  - moviebook-gateway
  http:
  - match:
    - uri:
        prefix: /ms
    route:
    - destination:
        host: cubews-mock.staging.svc.cluster.local
        port:
          number: 8080

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cubews-mock-staging-test
  labels:
    env: staging
spec:
  hosts:
  - staging.cubecorp.io
  gateways:
  - moviebook-gateway
  http:
  - match:
    - uri:
        prefix: /cube/ms
    rewrite:
        uri: /ms
    route:
    - destination:
        host: cubews-mock.staging.svc.cluster.local
        port:
          number: 8080

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cubews-replay-staging
  labels:
    env: staging
spec:
  hosts:
  - staging.cubecorp.io
  gateways:
  - moviebook-gateway
  http:
  - match:
    - uri:
        prefix: /rs
    - uri:
        prefix: /as
    route:
    - destination:
        host: cubews-replay.staging.svc.cluster.local
        port:
          number: 8080

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cubews-replay-staging-test
  labels:
    env: staging
spec:
  hosts:
  - staging.cubecorp.io
  gateways:
  - moviebook-gateway
  http:
  - match:
    - uri:
        prefix: /cube/rs
    rewrite:
        uri: /rs
    route:
    - destination:
        host: cubews-replay.staging.svc.cluster.local
        port:
          number: 8080

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cubews-analyze-staging-test
  labels:
    env: staging
spec:
  hosts:
  - staging.cubecorp.io
  gateways:
  - moviebook-gateway
  http:
  - match:
    - uri:
        prefix: /cube/as
    rewrite:
        uri: /as
    route:
    - destination:
        host: cubews-replay.staging.svc.cluster.local
        port:
          number: 8080

