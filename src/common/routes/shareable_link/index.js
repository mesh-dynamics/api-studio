import React from 'react'
import { Route } from 'react-router'
import ShareableLink from "./ShareableLink";

export default [
    <Route exact key="ShareableLink" path="/shareable_link" component={ShareableLink} />,
]
