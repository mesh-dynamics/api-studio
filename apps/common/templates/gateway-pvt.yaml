apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: {{namespace}}-pvt-gateway
  namespace: namespace
spec:
  selector:
    istio: pvt-ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"