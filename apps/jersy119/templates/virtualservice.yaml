apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: jersy119
  namespace: {{namespace}}
spec:
  hosts:
  - "{{namespace_host}}"
  gateways:
  - {{namespace}}-gateway
  http:
  - match:
    - port: 8080
    route:
    - destination:
        host: jersy119-record
        port:
          number: 8080
  - match:
    - port: 80
    route:
    - destination:
        host: jersy119-replay
        port:
          number: 8080
